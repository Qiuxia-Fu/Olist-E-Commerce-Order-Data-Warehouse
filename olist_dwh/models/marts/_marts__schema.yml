version: 2

models:
  - name: fct_order_items
    columns:
      - name: order_id
        tests: [not_null]
      - name: customer_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_customers')
                field: customer_id
      - name: product_id
        tests:
          - relationships:
              arguments:
                to: ref('dim_products')
                field: product_id

  - name: dim_customers
    columns:
      - name: customer_id
        tests: [not_null]

  - name: dim_products
    columns:
      - name: product_id
        tests: [not_null, unique]
